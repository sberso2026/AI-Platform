# Live validation

Authenticated founder screenshots at 1366×768 / 1440×900 / 1920×1080 require a magic-link session (`silvestre.berso@rtbea.com.au`). This pack does not invite external users.

Pre-repair unauthenticated probes proved HTML 500 on every generic PI intelligence route (`<!DOCTYPE`, `text/html`, Next `/500`) and JSON 401 on Engineering Command Center after EOS-COMMAND-500-1.

Post-repair certification:

1. Deploy the identifiable commit to Preview only.
2. Confirm unauthenticated PI APIs return JSON 401, not HTML.
3. Authenticated founder walkthrough of Overview, Schedule, Cost, Risk & Change, Engineering, Decisions, Reports, Ask Project Intelligence — no `Unexpected token '<'`.
4. Absent source data shows empty/unavailable, not an exception and not fake zeros.

Screenshots, if captured after founder login, belong in `screenshots/`.
