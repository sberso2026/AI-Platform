# EOS-SHELL-NAV-1 — Restore Engineering Systems Navigation

Preview only. Do not promote Production. Do not invite external users.

The Engineering Systems matrix remains the governed application launcher at the existing route `/engineering/modules`. This ticket restores the primary sidebar item; it does not create a new route.

## Change

- Label: Engineering Systems (internal id `eng-modules` unchanged)
- Placement: Engineering OS section, immediately after Command Centre
- Access: same Engineering OS audience as Command Centre — not gated on individual application entitlement
- Icon: existing `AppWindow`
- Active state: exact match on `/engineering/modules` only
