# EOS-SHELL-JARVIS-2 before / after

Composition change is the acceptance criterion. Color-only deltas fail.

| Surface | Before (JARVIS-1) | After (JARVIS-2) |
| --- | --- | --- |
| Command Center | Equal KPI card row | 3-column cockpit + live signals + recent change |
| Modules | SaaS product cards with module keys | Engineering systems matrix |
| PI Overview | Navigation card grid dominating the page | Operational cockpit + AI brief; records as compact rail |
| Empty project | Dead “Select a project” empty state | Project-selection command surface describing what activates |
| Visualizations | Status chips and text counts | Health ring, intelligence core, live signals, timeline, severity bars, evidence chain |
| Nav | Same dark treatment on both sidebars | Primary EOS nav darkest; PI context nav elevated with cyan active rail |
| Analyst | Form + stacked cards | Console prompt + structured executive response |

Authenticated side-by-side screenshots at 1366 / 1440 / 1920 belong in `screenshots/` after Preview deployment. Until founder visually accepts those captures, `EOS_ENTERPRISE_VISUAL_PASS=false`.
