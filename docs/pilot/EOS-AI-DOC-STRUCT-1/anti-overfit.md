# Anti-overfit

`tests/anti-overfit-structure.test.ts` scans production structure/QA sources for founder document numbers, clause IDs, values, and question strings.

`ANTI_OVERFIT_PASS=true` for that scan. Evaluation fixtures may contain expected values.
