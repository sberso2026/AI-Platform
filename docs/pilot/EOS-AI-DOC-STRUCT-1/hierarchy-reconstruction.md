# Hierarchy reconstruction

Parent numbered clauses keep lettered and roman children. Adjacent `(d)` and `(e)` are siblings, not unrelated windows. Duplicate-provenance collapse no longer treats two lettered children of the same numbered clause as the same span unless text overlap is high.
