# Continuation handling

`detectEvidenceCompleteness` / `checkEvidenceCompleteness` implement EVIDENCE_COMPLETENESS_CHECK.

Assembly pulls matching children before a parent “the following:” list is treated as complete. Unrelated siblings are omitted unless the question has no qualifying condition and multiple values apply.
