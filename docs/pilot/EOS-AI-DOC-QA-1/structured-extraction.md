# Structured extraction

Generic operators: MIN / MAX / EQ from `not less than`, `not exceeding`, `shall be`, `minimum`, `maximum`.

Facts include subject (nearby), property (from span cues), value, unit, clause, page.

Selection scores facts against the asked property/constraint. No hardcoded document values.
