name: Phase 10B Asset Intelligence Core

on:
  workflow_dispatch:
  push:
    branches:
      - master
      - main
      - release/**
      - cursor/**
    paths:
      - "packages/asset-intelligence/**"
      - "packages/asset-intelligence-certification/**"
      - "packages/platform-certification/src/phase10b*"
      - "docs/architecture/ASSET_INTELLIGENCE*"
      - ".github/workflows/phase-10b-asset-intelligence-core.yml"

concurrency:
  group: phase-10b-asset-intelligence-core-${{ github.ref }}
  cancel-in-progress: true

env:
  PLATFORM_CERTIFICATION: "1"
  NODE_VERSION: "22"
  PNPM_VERSION: "9.15.0"

jobs:
  certify:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Fetch release tags
        run: git fetch --tags --force
      - uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Unit tests
        run: pnpm --filter @rtb/asset-intelligence test
      - name: Architecture tests
        run: pnpm --filter @rtb/platform-certification exec vitest run src/phase10b-asset-intelligence-core.test.ts
      - name: Phase 10B certification (gates A–Y)
        run: pnpm --filter @rtb/asset-intelligence-certification certify:phase10b
      - name: Assert PASS artifact
        run: |
          node <<'EOF'
          const { readFileSync } = require("fs");
          const path = "packages/asset-intelligence-certification/artifacts/phase10b-asset-intelligence-core-certification.json";
          const a = JSON.parse(readFileSync(path, "utf8"));
          const checks = [
            ["verdict", a.verdict === "PASS"],
            ["failedGateCount", a.failedGateCount === 0],
            ["skippedGateCount", a.skippedGateCount === 0],
            ["notExecutedGateCount", a.notExecutedGateCount === 0],
            ["duplicateAssetOwnershipDetected", a.duplicateAssetOwnershipDetected === false],
            ["assetIdentityOwnership", a.assetIdentityOwnership === "engineering_os_shared_domain"],
            ["productionAssetIntelligenceReady", a.productionAssetIntelligenceReady === false],
            ["coreConditionSliceReady", a.coreConditionSliceReady === true],
            ["assetIntelligenceImplemented", a.assetIntelligenceImplemented === true],
            ["accuracyClaimsCertified", a.accuracyClaimsCertified === false],
            ["rulClaimsCertified", a.rulClaimsCertified === false],
            ["projectIntelligenceV1Intact", a.projectIntelligenceV1Intact === true],
            ["inspectionIntelligenceV1Intact", a.inspectionIntelligenceV1Intact === true],
            ["secretExposureDetected", a.secretExposureDetected === false],
            ["releaseEligible", a.releaseEligible === true],
            ["phase10CReady", a.phase10CReady === true],
            ["gatesExecuted", Array.isArray(a.requiredGates) && a.requiredGates.length === 25],
          ];
          const failed = checks.filter(([, ok]) => !ok).map(([n]) => n);
          if (failed.length) {
            console.error("Assert failed:", failed);
            process.exit(1);
          }
          console.log(JSON.stringify({ verdict: a.verdict, phase10CReady: a.phase10CReady }, null, 2));
          EOF
      - name: Upload certification artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: phase10b-asset-intelligence-core-certification
          path: packages/asset-intelligence-certification/artifacts/phase10b-asset-intelligence-core-certification.json
          if-no-files-found: warn
